#include <stdio. h>
void main ()
 {
   Ini n, fact=1,i;
   printf("Enter an integer\n");
   scanf("%d",&n); 
   if(n==0)
     printf("The factorial of 0 is 1\n");
   else
     {
       for(i=1;i<=n; i++)
         fact*=i;
      }
      printf("factorial of %d=%d",n, fact); 
}
    
