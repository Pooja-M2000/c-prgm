#include <stdio.h>
void main()
   {
      int i, n, a=0, b=1, c=0;
      printf("Enter the num of elements \n");
      scanf("%d", &n);
      for(i=0;i<n;i++)
        { 
           printf("%d\t", c);
           a=b;
           b=c;
            c=a+b;
        }
  }
